{
  "name": "@meetingloop/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@meetingloop/ai": "workspace:*",
    "@meetingloop/db": "workspace:*",
    "@meetingloop/domain": "workspace:*",
    "@meetingloop/queue": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.20.3"
  }
}
