{
  "name": "@meetingloop/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "db:migrate": "node ./scripts/migrate.mjs",
    "db:seed": "node ./scripts/seed.mjs"
  },
  "dependencies": {
    "@meetingloop/auth": "workspace:*",
    "@meetingloop/domain": "workspace:*"
  }
}
